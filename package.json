{
  "name": "pharmconsilium-site",
  "private": true,
  "scripts": {
    "build:js": "node scripts/build-pharm-bundle.mjs",
    "build": "node scripts/build-deploy.mjs",
    "check:redirects": "node scripts/check-redirects.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
